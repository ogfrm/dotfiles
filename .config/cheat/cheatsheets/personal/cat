# To display the contents of a file:
cat <file>

# To display file contents with line numbers
cat -n <file>

# To display file contents with line numbers (blank lines excluded)
cat -b <file>

########################################
cat file1 file2 #shows both files
cat file | sort | uniq

cat -n file1 #shows line number
cat -A file  # shows all char including spaces tabs EOF
cat file1 | grep --color findtext
cat file1 | wc -l  #line count

cat <<EOF | sudo tee /etc/systemd/system/regenerate_ssh_host_keys.service
lines
.
.
..


EOF
