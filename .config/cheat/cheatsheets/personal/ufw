sudo ss -tupln #See open ports
apt install ufw
sudo ufw status
sudo ufw allow 777
sudo ufw enable
sudo ufw reload
#Test you can enter from other command
sudo ufw allow 80/tcp

#_____ Drop pings Disable Ping
sudo nano /etc/ufw/before.rules
#	Add this line after # ok icmp codes for INPUT
-A ufw-before-input -p icmp --icmp-type echo-request -j DROP
#Test ping

# Installation of the firewall etc
apt install -yq4 ufw
# Set up the necessary ports in the firewall
# Let's Encrypt:
ufw=$(which ufw)
ufw allow 80/tcp comment "LetsEncrypt(http)"
ufw allow 443/tcp comment "TLS(https)"
ufw allow 22/tcp comment "SSH"
ufw logging medium && ufw default deny incoming && ufw enable
service ufw restart