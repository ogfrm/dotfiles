# To see running processes:
systemctl

# To check the status of a service:
systemctl status foo.service

# To start/restart/stop a service:
systemctl start/restart/stop foo.service

# To reload a service's configuration:
systemctl reload foo.service

# To edit a service's configuration:
systemctl edit foo.service

# To reload systemd manager configuration:
systemctl daemon-reload

# To enable a service to startup on boot:
systemctl enable foo.service

# To disable a service to startup on boot:
systemctl disable foo.service

# To start/restart/stop per-user service:
systemctl --user start/restart/stop emacs.service

# To see all active units, add --all for everything:
systemctl list-units

# To see all service units:
systemctl list-units -at service

# To see filtered units (all running service):
systemctl list-units -t service --state running

# To see all service files, see which are enabled or disabled:
systemctl list-unit-files -at service

# To list all units with specific status, inactive, active, enabled, running, exited:
systemctl list-units --all --state=inactive

# To use systemctl to list all unit files:
systemctl list-unit-files

# To see log items from the most recent boot:
journalctl -b

# To to see only kernal messages, add -b for at the most recent boot:
journalctl -k

# To get the log entries for a service since boot:
journalctl -b -u foo.service

# To list the dependencies of a service:
# when no service name is specified, lists the dependencies of default.target
# add -all to expand dependencies recursively
systemctl list-dependencies foo.service

# To see low level details of a service settings on the system:
systemctl show foo.service

# To list currently loaded targets:
systemctl list-units --type=target

# To change current target:
systemctl isolate foo.target

# To change default target:
systemctl enable foo.target


###########################################
#systemd
systemctl status app#
sudo systemctl diasable apache2 #dont start automaticly
sudo systemctl stop apache2
sudo systemctl start apache2
sudo systemctl restart apache2
sudo systemctl enable apache2
sudo systemctl  #listem all services
sudo systemctl list-units
sudo systemctl list-units --all
sudo systemctl list-units -t service
sudo systemctl list-unit-files --type=service #listem all services
sudo systemctl reload apache2  #it will not stop service first
sudo systemctl reload-or-restart apache2
sudo systemctl is-active apache2
sudo systemctl is-enabled apache2

sudo systemctl status syncthing@user
cd /usr/lib/systemd/system
ls #for services
nano syncthing@.services
    After=network.target   #start after network

    ExecStart= command

Vendor Preset is for when service installed
sudo journalctl -xe
sudo systemctl restart journald

systemctl vm.swappiness  #linux swap use system must use

You can list all services running with:
systemctl list-unit-files

## service files

unit files .service, .mount, .target, , .socket , .timer
/etc/systemd/system  (most common highest priority)
/run/systemd/system  (runtime service files)
/lib/systemd/system  (installed service files  lowest priority)

cat /etc/systemd/system/apache2.service
[Unit] (general info require and after and before )
[Service] Type: simple,notify,forking
    ExecStart=  what to do when start
    ExecReload= what to do when reload reload config without stop
[Install]
    what happens enabled disabled
    Wantedby: require

## systemctl edit apache2.service

you can only create file that only changes like only Description
you have to put then above the line it say to write

## systemctl edit --full apache2.service

it will copy the org file firt then you can edit
