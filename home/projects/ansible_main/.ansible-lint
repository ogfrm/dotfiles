---
# .ansible-lint
# profile: min # min, basic, moderate,safety, shared, production
# exclude_paths:
  # - roles/
skip_list:
#   - line-length
  - var-naming[no-role-prefix]
# warn_list:
#   - line-length
  # - role-name